info:
  name: put update investment
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/investments/:id"
  body:
    type: json
    data: |-
      {
        "expected_gain": 20.0,
        "risk_level": "high",
        "status": "closed"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
