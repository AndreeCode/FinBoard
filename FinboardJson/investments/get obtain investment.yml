info:
  name: get obtain investment
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/investments/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
