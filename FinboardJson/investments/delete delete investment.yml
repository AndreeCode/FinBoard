info:
  name: delete delete investment
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/investments/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
