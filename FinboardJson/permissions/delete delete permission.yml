info:
  name: delete delete permission
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/permissions/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
