info:
  name: put update permission
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/permissions/:id"
  body:
    type: json
    data: |-
      {
        "name": "update_categories",
        "description": "Permission to update categories"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
