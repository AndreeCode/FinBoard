info:
  name: delete delete credit
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/credits/{{credit_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
