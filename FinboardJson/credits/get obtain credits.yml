info:
  name: get obtain credits
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/credits"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
