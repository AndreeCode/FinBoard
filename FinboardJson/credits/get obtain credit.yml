info:
  name: get obtain credit
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/credits/{{credit_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
