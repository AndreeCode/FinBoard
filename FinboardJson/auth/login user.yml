info:
  name: login user
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email":"admin@finboard.com",
        "password":"admin123"
      }
  auth: inherit
  cookies:
    send: true
    save: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
