info:
  name: get me
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/auth/me"
  auth: inherit
  cookies:
    send: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
