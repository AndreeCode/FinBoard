info:
  name: auth logout
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/auth/logout"
  auth: inherit
  cookies:
    send: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
