info:
  name: get obtain transaction
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/transactions/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
