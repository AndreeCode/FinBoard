info:
  name: delete delete category
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/categories/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
