info:
  name: get obtain category
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/categories/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
