info:
  name: get obtain user
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/users/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
