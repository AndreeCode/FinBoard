info:
  name: get obtain role-permissions
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/role-permissions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
