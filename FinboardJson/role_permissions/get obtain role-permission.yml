info:
  name: get obtain role-permission
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/role-permissions/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
